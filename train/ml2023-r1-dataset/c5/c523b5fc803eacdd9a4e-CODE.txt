python
def sum_list(lst):
    if len(lst) == 0:      # базовый случай: если список пустой, возвращаем 0
        return 0
    else:
        return lst[0] + sum_list(lst[1:])   # рекурсивный случай: суммируем первый элемент и вызываем функцию для остального списка
