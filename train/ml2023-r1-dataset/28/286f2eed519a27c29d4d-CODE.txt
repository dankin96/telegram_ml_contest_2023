
#include <iostream>
#include <cmath>
#include <stdio.h>

 using namespace std;

 int main()
{
    double x=0, y=0, z=0;
    cin>>x>>y;
    z=log(abs(pow(x+y,2.)+ sqrt(abs(y)+2)-( x- x*y/(x*x/2-5))))+pow(cos(x+y),2)/pow(x+y, 1./3.);
    printf("%.2f",z);